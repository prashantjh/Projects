Deciding which variables to keep. There are 290 variables in the data, not all of them are useful.
Following steps will be crucial in selecting the variables. We will start with only a few variables ~30 then keep on adding more.
